{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "User Instruction To ActionDecision Flow",
  "description": "从用户自然语言任务输入开始，按源码真实顺序阅读 PlanningModule 到 ActionDecisionModule 的主流程。描述尽量用高中生都能听懂的话解释，并穿插典型例子。",
  "steps": [
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 151,
      "description": "先看 `PlanningModule.Start()`。这里把记忆、LLM、智能体属性和通信模块都接上。你可以把它理解成：先把“大脑、嘴巴、耳朵、身份信息”都插好电。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 121,
      "description": "再看 `ActionDecisionModule.Start()`。动作层在这里接上 `PlanningModule`、地图网格、MLAgents 控制器和感知状态。前者负责‘想做什么’，后者负责‘真的怎么动’。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 172,
      "description": "`SubmitMissionRequest()` 是用户指令入口。比如用户说“3 台无人机去图书馆和行政楼巡查”，这句话会从这里进入系统。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 1233,
      "description": "`AnalyzeMissionDescription()` 是总任务理解主流程。它不直接让 LLM 输出动作，而是分两步：先拆阶段，再拆成每个智能体的槽位任务。这样更稳。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 1295,
      "description": "`ExtractMissionPhasePlan()` 处理第一轮 LLM 返回。比如用户说“先侦查，再汇合”，这里会先把它整理成阶段结构，而不是立刻决定具体坐标。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 1410,
      "description": "`ExtractMissionPhasePlan()` 内部真正关心的是：任务被分成几段、每段大概要多少智能体、谁依赖谁。它解决的是‘大步骤怎么排’，不是‘具体去哪儿’。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 1526,
      "description": "`NormalizePhasePlan()` 给阶段计划补默认值、修正错误依赖、保证数量合法。简单说，就是把 LLM 的‘草稿答案’整理成老师看得懂的规范答案。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 1391,
      "description": "阶段计划准备好后，`AnalyzeMissionDescription()` 发起第二轮拆解。现在问题从‘任务分几阶段’变成‘每个智能体最后到底负责哪个目标’。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 226,
      "description": "`ParseMissionFromLLM()` 把第二轮结果组装成 `MissionAssignment`。最关键的字段是 `taskSlots`，因为后面动作层主要就认这里面的 `target` 和 `viaTargets`。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 2259,
      "description": "`ExtractMissionAnalysisResponse()` 是任务级 JSON 提取入口。用户原始中文从这里开始慢慢变成结构化字段。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 2277,
      "description": "`TryParseMissionAnalysisResponse()` 真正解析任务级对象。它希望 LLM 明确给出 `roles`、`taskSlots`、`coordinationDirectives`，而不是只写一大段解释。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 2823,
      "description": "`ExtractTaskSlotsFromResponse()` 是目标解析最关键的一层。对系统来说，`slot.target` 是最终目标，`slot.viaTargets` 是先经过的中间点。例子：‘先到一食堂再去图书馆北门’应该变成 `target=图书馆北门, viaTargets=[一食堂]`。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 2890,
      "description": "`EnsureTaskSlotsAreExecutable()` 检查槽位能不能真的执行。它会修掉重复 ID、坏依赖，还会处理一个常见问题：LLM 把一条完整任务错误拆成很多段中间槽位。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 2991,
      "description": "`CompressTaskSlotsToEndToEndAssignments()` 专门解决‘把检查点当终点’的问题。比如 phase_1 是“先飞到 A”，phase_2 是“观察 B”，这里会尽量压成 `target=B, viaTargets=[A]`。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 3237,
      "description": "`BuildTaskSlotsForMission()` 是最保守兜底。如果 LLM 没给清楚目标，系统只能把整句任务描述先塞进 `target`。这能跑，但目标会很模糊。比如‘去巡逻’就可能只剩一整句模糊文本。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 178,
      "description": "任务分配完成后，`ReceiveMissionAssignment()` 把任务送到每个智能体。第一次进来时，如果还没确定具体岗位，先不做个人计划，而是先上报角色偏好。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 178,
      "description": "第二次回到 `ReceiveMissionAssignment()` 时，`specificRole` 和 `specificSlot` 已经有了。这时系统已经知道‘你是谁、你的槽位终点是谁’，所以可以开始做个人步骤计划。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 344,
      "description": "`AnalyzeMissionAndCreatePlan()` 让 LLM 把当前槽位变成 3 到 5 步的个人计划。注意这里 prompt 明确要求把目标写进 `stepIntents.primaryTarget`，把路过点写进 `orderedViaTargets`。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 473,
      "description": "`ParseAndCreatePlan()` 把个人计划真正落到 `currentPlan`。从这里开始，系统手里同时有三层目标：任务槽位目标、步骤目标、协同共享目标。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 1757,
      "description": "`ExtractStepIntentsFromResponse()` 解析每一步的结构化意图。你可以把它理解成：把‘步骤句子’继续翻译成‘这一步到底要对谁做事’。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 1857,
      "description": "`BuildFallbackStepIntents()` 是保守兜底。LLM 如果没写清 `primaryTarget`，系统不会重新读中文乱猜，而是尽量直接用 `assignedSlot.target`。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 1910,
      "description": "`NormalizeStepIntent()` 负责补空。常见补法是：`primaryTarget` 为空时，用槽位终点补；`orderedViaTargets` 为空时，用槽位里的检查点补。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 1961,
      "description": "`RepairStepIntentsAgainstAssignedSlot()` 解决一个非常现实的问题：模型容易把中间经过点写成最终目标。比如本来应该‘先到一食堂，再观察图书馆’，结果模型可能把后面观察步骤也写成一食堂，这里会把它纠回图书馆。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 980,
      "description": "`GetCurrentStepIntent()` 是动作层之后最常读的接口。ActionDecision 不再直接读用户原话，而是优先读这里返回的结构化步骤目标。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 998,
      "description": "`GetCurrentStepRoutePolicy()` 给动作层拿路径策略，比如是否偏左接近、是否优先 A*。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 1015,
      "description": "`GetCurrentCoordinationDirectives()` 给动作层拿协同约束。比如‘和谁同步’、‘共享目标是谁’。如果个人步骤没写目标，动作层有时还会退到这里找共享目标。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 3429,
      "description": "`GetCurrentAssignedSlotSummary()` 对动作层暴露槽位摘要。它像一张岗位卡：你负责哪个槽位、终点是谁、先经过谁、完成条件是什么。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 1107,
      "description": "`DecideNextAction()` 是动作层主入口。可以把它理解成：PlanningModule 已经给出‘这一步做什么’，现在 ActionDecision 要决定‘这一轮到底发哪些原子动作’。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 3764,
      "description": "一开始 `DecideNextAction()` 先查 `planningModule.HasActiveMission()`。如果协调者还没放行、或者任务已经结束，动作层就不能乱动。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 227,
      "description": "`BuildStepNavigationDecision()` 先判断这一步是不是移动步、是否允许 A*。比如‘观察图书馆’更像观察步，‘飞到图书馆’才更像移动步。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 425,
      "description": "`ResolveStructuredStepTargetBinding()` 是结构化目标绑定入口。优先级非常重要：`stepIntent.primaryTarget` > `assignedSlot.target` > `coordination.sharedTarget`。这意味着动作层尽量相信上游整理好的表格，而不是再猜中文。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 2373,
      "description": "`BuildHighLevelDecisionPrompt()` 让 LLM 只做一件事：决定当前这一轮要做哪些高层动作。它被明确要求优先写 `step_target`，也就是“别自己发明新目标，优先用系统绑定好的目标”。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 2345,
      "description": "`ParseActionDataSequenceFromJSON()` 把高层动作 JSON 解析成 `ActionData`。这一步只解析动作数据，不负责把目标变成坐标。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 2531,
      "description": "`ResolveStepTargetBindingFromActionData()` 是一个保险机制。正常情况下，它优先继续用 PlanningModule 给的结构化目标；只有上游完全没给目标时，才允许吃高层动作里显式写出来的新目标。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 2577,
      "description": "`TryBuildStepTargetBindingFromTarget()` 是目标翻译器。它会尝试把一段目标文本翻译成真实对象。例子：`图书馆` -> Feature，`world(10,20)` -> World，`Agent_2` -> Agent。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 3342,
      "description": "`TryResolveCampusFeatureTarget()` 负责把地点名称变成真实地点。它支持“building:图书馆”“feature:行政楼”或者直接写名称，还会先看当前感知，再看全局 CampusGrid。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 263,
      "description": "`ResolveEffectiveStepIntent()` 把 PlanningModule 的 stepIntent 和动作层当前绑定结果合并一下。如果步骤目标没写清，但已经有稳定绑定，就把绑定的原始查询补回当前步骤意图。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 305,
      "description": "`ResolveEffectiveRoutePolicy()` 把上游给的路径策略拿过来。比如‘从北侧接近’、‘尽量绕开障碍’这些要求，会在后面下发到动作参数里。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 497,
      "description": "`BuildCoarsePathContextForStep()` 会尝试先算一条 A* 粗路径。它特别重视 `viaTargets`，所以如果目标是‘先到 A 再去 B’，粗路径会尽量保住这些结构化检查点。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 2792,
      "description": "`BuildActionCommandsFromActionData()` 把高层动作翻译成真正可执行的 `ActionCommand`。例如 LLM 说 `MoveTo step_target`，这里会继续往下变成具体动作对象。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 3052,
      "description": "`ParseSingleAction()` 是单个动作的落地入口。这里会真正解析动作类型和目标来源。最典型的情况是：如果 `MoveTo` 没写具体坐标，就优先自动补 `lastStepTargetBinding.WorldPos`。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 3220,
      "description": "`TryResolveTargetFromAllowedSources()` 是动作目标白名单解析器。它只允许几类目标：`step_target`、world 坐标、地点、感知小节点、队友。这样做是为了防止 LLM 随口编一个不存在的地点。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 3318,
      "description": "`TryResolveStepTargetReference()` 专门处理 `step_target`。如果动作写的是 `step_target`，这里会直接把它替换成前面已经绑定好的真实坐标。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 1922,
      "description": "`ApplyRoutePolicyToActionSequence()` 把路径策略塞进动作参数。比如 `viaTargets`、`approachSide`、`altitudeMode` 都会在这里下放到具体移动动作上。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 1969,
      "description": "`ApplyCoordinationDirectivesToActionSequence()` 把协同约束也下发到动作参数。比如共享目标、让行对象、同步点，会跟着动作一起往执行层走。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 1859,
      "description": "`ApplyCoarsePathFallbackIfNeeded()` 处理一个常见异常：LLM 没给 waypoint，但系统已经算出了粗路径。这时系统会把粗路径塞进第一条 `MoveTo`，避免‘知道怎么走却没法执行’。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 1632,
      "description": "`ExpandMoveActionsByAStar()` 把移动动作进一步展开成多个短 waypoint。优先级是：系统粗路径 > LLM waypoint > A* 兜底。可以理解成：先信结构化路线，再信 LLM 补点，最后才让系统自己现算。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 3578,
      "description": "`ExecuteActionSequence()` 开始真的执行动作。动作一条条跑，失败就重新决策；成功后再看这一步是不是已经真正到目标了。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 3685,
      "description": "`ShouldContinueCurrentStepWithoutCompletion()` 解决‘动作执行完了，但人其实还没到目标’的问题。比如只走完了 A* 前两段 waypoint，还没到图书馆门口，就不能提前推进到下一步。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 3702,
      "description": "`RefreshStepTargetBindingForCompletion()` 对动态目标特别重要。比如跟随队友时，队友会动，所以完成判定前要先刷新一次目标位置。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 3721,
      "description": "`IsNearStepTarget()` 真正判断“到没到”。静态建筑看距离，建筑还会用更大的到达半径；无人机还会额外看高度差。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 3677,
      "description": "动作层确认这一步真做完后，会调用 `PlanningModule.CompleteCurrentTask()` 推进到下一步。也就是说：Planning 负责步骤进度，ActionDecision 负责把当前步骤真正做完。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 3660,
      "description": "最后回到 `ExecuteActionSequence()` 的收尾。如果 `planningModule.HasActiveMission()` 仍然为真，就进入下一步；如果本地做完但队友还没做完，就等待全队收口。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 2823,
      "description": "带着例子回头总结一次。如果用户说“先到一食堂，再去图书馆北门观察”，理想链路是：`taskSlot.target=图书馆北门`，`taskSlot.viaTargets=[一食堂]`，`stepIntent.primaryTarget=图书馆北门`，动作里优先写 `step_target`。"
    },
    {
      "file": "LLM_Modules/PlanningModule.cs",
      "line": 3237,
      "description": "再看模糊情况。如果用户只说“去巡逻”“去那边看看”，而 LLM 也没成功产出明确目标，那么系统通常只能退到模糊 `target` 或空绑定。这种情况能跑通流程，但目标精度明显下降。"
    },
    {
      "file": "LLM_Modules/ActionDecisionModule.cs",
      "line": 425,
      "description": "最后记住一句最核心的话：ActionDecision 不想重新猜用户原话。它优先只信 PlanningModule 已经整理好的结构化目标。如果上游结构清楚，动作层就很稳；上游结构模糊，动作层也会跟着模糊。"
    }
  ]
}
